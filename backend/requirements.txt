# Backend Python dependencies — verified working on Python 3.11+, 3.13+
#
# NOTE: torch is installed as CPU-only to keep the image lightweight.
# The ML packages pull torch automatically; to get CPU-only explicitly:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Data validation
pydantic==2.8.2
pydantic-settings==2.4.0

# Async HTTP / HTTP client
httpx==0.27.0
requests>=2.32.5

# LLM & Embeddings — pinned to latest stable compatible versions
groq>=0.11.0
langchain>=1.0.0
langchain-community>=0.4.0
langchain-core>=1.0.0
langchain-groq>=1.0.0
langchain-huggingface>=1.0.0
langchain-text-splitters>=1.0.0

# Vector store
pinecone>=4.0.0

# Embeddings & ML
sentence-transformers>=3.0.0
scikit-learn>=1.5.0

# PDF processing
pypdf==4.3.1

# Keyword search
rank-bm25==0.2.2

# Web search (agent tool)
duckduckgo-search>=6.0.0

# Database
aiosqlite==0.20.0
sqlalchemy>=2.0.0

# Utilities
orjson>=3.9.0
numpy>=1.26.0
python-dotenv==1.0.1
structlog>=24.0.0
anyio>=4.0.0
